#include "main.h"
/**
 * _isdigit - checks for a digit (0 through 9).
 *
 * @c: check for input
 *
 * Return: Returns 1 if c is a digit
 * Returns 0 otherwise
 *
 */
int mul(int a, int b)
{
	int mul;

	putchar(mul + 48);
}
