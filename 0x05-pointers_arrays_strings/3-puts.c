#include <stdio.h>
#include "main.h"

/**
 * _puts -  returns the length of a string.
 *
 * @str: input pointer to a
 *
 */
void _puts(char *str)
{
	int i;

	for (i = 0; s != '\0'; s++)
		printf("%c", *str);
	printf("\n");
}
