#include "main.h"
/**
 * mul - function that multiplies two integers.
 *
 * @a: check for input
 * @b: check for input
 *
 * Return: Returns 0 success
 *
 */
int mul(int a, int b)
{
	int mul;

	putchar(mul + 48);
return (0);
}
