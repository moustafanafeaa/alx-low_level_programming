#include "main.h"
/**
 * mul - function that multiplies two integers.
 *
 * @a: check for input
 * @b: check for input
 *
 * Return: Returns 0 success
 *
 */
int mul(int a, int b)
{
	int mult;

	mult = a + b;
	putchar(mult + 48);
return (0);
}
