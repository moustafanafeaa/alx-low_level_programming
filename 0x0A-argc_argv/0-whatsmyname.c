#include <stdio.h>
#include "main.h"
/**
 * main - entry point its name, followed by a new line.
 * @argc: counter to arguments.
 * @argv: array of args.
 * Return: return 0 success.
 */
int main(int __attribute__ ((unused)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
